import { Footer, Breadcrumb, Box } from 'react-bulma-components'

export default function Bottom() {
    return (
        <Footer style={{
            marginTop: '10rem'
        }}>
        </Footer>
    );
};